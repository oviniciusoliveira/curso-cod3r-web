// CÓDIGO NÃO EXECUTÁVEL!

// Procedural
processamento(valor1, valor2, valor3)

// Orientação a Objetos
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}   

objeto.processamento() // Foco passou a ser o objeto

// Princípios importantes:
// 1. Abstração para formulação de objetos
// 2. Encapsulamento: esconder os detalhes de implementação. exibindo apenas a interface de comunicação
//      Baixo nível de encapsulamento resulta em uma maior dependência entre os objetos
// 3. Herança (prototype) 'relação é um...'
//      ? Priorizar Composição à Herança ?
// 4. Polimorfismo