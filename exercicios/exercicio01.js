function calcular(a, b) {
    soma = a + b
    sub = a - b
    mult = a * b
    div = a / b

    console.log(`Soma: ${soma} Subtração: ${sub} Multiplicação: ${mult} Divisão: ${div}`)
}

calcular(3, 8)
